# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ../datasets/cubetower # dataset root dir
train: train_set/images # train images (relative to 'path') 128 images
val: val_set/images # val images (relative to 'path') 128 images
test: # test images (optional)

# Classes (80 COCO classes)
names:
    0: Eiffel Tower
    1: Rubiks Cube
